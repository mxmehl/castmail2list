# App settings
SQLALCHEMY_DATABASE_URI: "sqlite:///castmail2list.db"
SECRET_KEY: "your-secret-key-here"

# General settings
LANGUAGE: "en"  # Supported languages: "en", "de"
DOMAIN: "***REMOVED***"
HOST_TYPE: "uberspace7"  # Can be: uberspace7, uberspace8
CREATE_LISTS_AUTOMATICALLY: true

# IMAP settings and defaults (used as defaults for new lists)
IMAP_DEFAULT_HOST: "***REMOVED***"
IMAP_DEFAULT_PORT: 993
IMAP_DEFAULT_PASS: "testtest123"

# IMAP folder names
IMAP_FOLDER_INBOX: "INBOX"
IMAP_FOLDER_PROCESSED: "Processed"
IMAP_FOLDER_SENT: "Sent"
IMAP_FOLDER_BOUNCES: "Bounces"
IMAP_FOLDER_DENIED: "Denied"
IMAP_FOLDER_DUPLICATE: "Duplicate"

# SMTP settings (defaults for new lists)
SMTP_HOST: "***REMOVED***"
SMTP_PORT: 587
SMTP_USER: "test-list@***REMOVED***"
SMTP_PASS: "testtest123"
SMTP_STARTTLS: true

# Polling interval in seconds
POLL_INTERVAL_SECONDS: 10
